"""Abstract base interface for platform advertisement adapters."""

from abc import ABC


class BaseAdapter(ABC):
    """Contract for platform-specific advertisement adapters."""

    # TODO: Declare abstract methods for authenticate, create/update/pause ads,
    # and other shared platform operations once contracts are defined.
