[project]
name = "verisure-ad-automation"
version = "0.1.0"
description = "Multi-platform ad automation platform for VeriSure."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.141.1",
    "pydantic-settings>=2.14.2",
    "uvicorn>=0.52.0",
]

[tool.uv]

dev-dependencies = []
